/**
 * Orchestrator — reads inputs, sequences steps. Wired together in F13+.
 * For now: returns immediately (scaffold).
 */
export async function run(): Promise<void> {
  // TODO: implement in F2–F13
}
